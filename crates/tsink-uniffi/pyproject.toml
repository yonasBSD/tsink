[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "tsink"
requires-python = ">=3.8"
description = "Python bindings for tsink time-series database"
license = { text = "MIT" }

[tool.maturin]
bindings = "uniffi"
